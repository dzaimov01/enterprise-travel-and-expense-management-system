# enterprise-travel-and-expense-management-system
Enterprise Travel &amp; Expense Management System

## Docker

Run API + SQL Server with one command:

```bash
docker compose up --build
```

API URL: `http://localhost:8080`
